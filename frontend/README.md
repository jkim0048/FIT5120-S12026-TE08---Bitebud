# BiteBud frontend

Vue 3 + Vite + TypeScript. Run from the **monorepo root** (`../`): `npm run dev` or `npm run dev:frontend`.

See the root [README.md](../README.md) for environment setup and `frontend/.env.production.example` for production API origin.
