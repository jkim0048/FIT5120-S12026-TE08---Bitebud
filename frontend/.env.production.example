# Copy to .env.production before `npm run build` if the deployed API is not same-origin.
# No trailing slash. Example: https://your-service-xxxxx-region.run.app
VITE_API_ORIGIN=

# Client-side site gate (required for app routes). Set a strong value for production.
VITE_SITE_ACCESS_PASSWORD=
