# Copy to `.env` for local Vite. Do not commit real secrets.
# Site gate: must match what you type on /unlock (no bypass when unset).
VITE_SITE_ACCESS_PASSWORD=change-me-for-local-dev

# Optional: deployed API origin (no trailing slash)
# VITE_API_ORIGIN=
