{
  "name": "bitebud",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:migrate": "npm run prisma:deploy --prefix backend",
    "test:prep": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run db:migrate",
    "build:frontend": "npm run build --prefix frontend",
    "deploy:backend": "powershell -ExecutionPolicy Bypass -File scripts/deploy-backend.ps1",
    "deploy:hosting": "npm run build:frontend && npx firebase deploy --only hosting --project bitesbud-6bb88"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "firebase-tools": "^14.0.0"
  }
}
